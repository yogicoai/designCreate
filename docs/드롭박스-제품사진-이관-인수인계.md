# 드롭박스 제품사진 → 레퍼런스 이관 · 인수인계

작성 2026-09-21 · cdApi 세션에서 작업한 내용을 imgCreate 세션으로 넘김

---

## 1. 무엇을 하려던 건가

요기보 팀 드롭박스 `1. 디자인/2.7 제품사진` 에 제품사진이 **9,565장** 쌓여 있는데 imgCreate 레퍼런스에는 안 들어와 있다.
이걸 **원본은 손대지 않고 복사만 해서** cafe24 FTP 에 올리고 레퍼런스로 등록하는 작업.

사용자 조건: **원본(드롭박스)을 절대 건드리지 말 것.** → 읽기 전용으로만 접근한다. 쓰기·이동·삭제 코드 경로가 스크립트에 없다.

---

## 2. 지금 상태 (중단됨)

| | |
|---|---|
| 등록 완료 | **393장** — 미디 265 · 허기보 97 · 버블 31 |
| 1차 배치 목표 | 1,687장 (8개 폴더) |
| 남은 것 | **1,294장** |
| 중단 사유 | 업로드 프로세스가 exit 127 로 죽음. 직전까지 정상 동작(150/1466)했고 데이터 손상은 없음 |

**재개는 안전하다.** `sourcePath` 기준 멱등 upsert 라 재실행하면 이미 올린 것은 건너뛴다.

> ⚠ **재개하기 전에 3번의 미해결 이슈를 먼저 결정할 것.** 그냥 재개하면 레퍼런스에 1,294장이 더 들어간다.

---

## 3. 결정됨 — 별도 컬렉션으로 분리 (2026-09-21)

레퍼런스가 이미 4,624장이라 드롭박스 9,565장을 섞으면 기존 작업(촬영 1,072 · 모델컷 1,257)이 묻힌다.
**사용자 결정: 레퍼런스가 아니라 별도로 뺀다.**

| 항목 | 결정 |
|---|---|
| 저장소 | **`dropbox_assets`** 별도 컬렉션 |
| 화면 | **`/dropbox`** — 자산 관리 > 레퍼런스 **바로 아래** 「드롭박스」 메뉴 |
| FTP 경로 | **`/web/design/product/<슬러그>/` 그대로 유지** — 이미 올라간 393장을 안 건드려도 된다. 출처는 컬렉션과 `source` 필드로 구분한다 |
| 레퍼런스 쪽 | `category: 'dropbox'` 선택지 제거 — 이제 references 에 드롭박스 건은 하나도 없다 |

**이미 반영 완료.** 393장은 `dropbox_assets` 로 옮겼고 references 는 4,624장으로 돌아왔다.

## 4. 만든 것 / 고친 것

### 새 파일
- **`scripts/import-product-photos.mjs`** — 수집 → FTP 업로드 → Mongo 등록. 드라이런이 기본.
- **`scripts/check-product-photos.mjs`** — 등록된 URL 이 실제로 열리는지 검증(폴더별 현황 + HEAD 확인).

### 고친 파일
- **`src/lib/queries.ts`** `normalizeRefCategory` 에 `'dropbox'` 추가.
  (처음엔 `'product'` 로 넣었다가 사용자 요청으로 `'dropbox'` 로 바꿨다)
- **`src/components/ReferencesManager.tsx`** `CATEGORY_OPTIONS` 에 `{ value: 'dropbox', label: '드롭박스 파일' }` 추가.

### ✅ 393장 마이그레이션 완료

옛 코드로 시작돼 `category: 'product'`, `tags: ['product', 폴더명]` 으로 들어가 있던 393장을
`dropbox_assets` 로 옮기면서 `category`/`tags` 를 버렸다 — 컬렉션 자체가 '드롭박스 파일'이라
분류 칸이 필요 없고, 폴더명을 태그로 두면 분류처럼 보여서 오히려 해롭다.

`scripts/migrate-dropbox-assets.mjs` 가 그 일을 한다 (드라이런 기본, `--go` 실행, `--rollback --go` 되돌리기).
인덱스도 같이 만든다: `sourcePath`(unique) · `url` · `folderHint+sourcePath` · `labelStatus`.

## 5. 설계 결정 — 라벨을 일부러 확정하지 않았다

사용자가 **"폴더에 제품 정보가 정확히 입력됐다는 보장이 없다"** 고 지적했고, 실측해보니 사실이었다.

파일명에 영문 제품명이 박힌 644장으로 폴더명을 대조한 결과:

| 폴더 | 일치율 | 비고 |
|---|---|---|
| 미디·더블·피라미드·팟·허기보·버블 | 100% | 신뢰 가능 |
| 라운저 96% · 서포트 94% | 높음 | 신뢰 가능 |
| 맥스 77% · 미니 75% | 주의 | |
| **슬림** | **18%** | **41장이 파일명에 `midi`** |
| **롤 닷** | **0%** | 6장 전부 `LG-miniroll-*` |
| 롤맥스 33% · 카터필러_롤미디 25% · 인물 0% · 프라임 0% | 낮음 | |
| 샐리필·도기보·롤메이트·모듀·메이트·오토만 | 판정불가 | 파일명 단서 0 |

게다가 **파일명 단서가 있는 건 전체의 7%뿐**이고, 나머지 93%는 `DSC00145.jpg` 같은 카메라 일련번호다.
그리고 한 장에 여러 제품이 나오는 사진이 흔하다(`simu_sp_image_support_pyramid_aquablue.png`) —
**"사진 1장 = 제품 1개"라는 전제 자체가 틀렸다.**

→ 그래서 **`sub`(확정 라벨)를 `null` 로 비워두고 근거만 따로 적는 구조**로 갔다.
나중에 검수 화면에서 `labelStatus: 'conflict'` 부터 훑어 `sub` 를 채우면 된다.
`sourcePath` 에 드롭박스 원본 경로가 남아 있어 언제든 되돌아갈 수 있다.

---

## 6. 데이터 스키마 (`dropbox_assets` 컬렉션)

```js
{
  url:          'https://yogibo.openhost.cafe24.com/web/design/product/midi/midi_0001.jpg',
  title:        'DSC_0128',          // 원본 파일명(확장자 제외)
  width: 0, height: 0,               // 미측정 — 필요하면 나중에 백필
  sub:          null,                // 확정 제품 라벨 — 검수 화면에서 채운다
  folderHint:   '미디',               // 근거① 드롭박스 폴더명 (분류로 쓰지 말 것)
  filenameHint: ['midi'],            // 근거② 파일명에서 찾은 제품 토큰
  labelStatus:  'agree',             // agree | folder-only | conflict
  sourcePath:   '미디/DSC_0128.jpg',  // 드롭박스 원본 상대경로 — 멱등 키 (unique 인덱스)
  sourceName:   'DSC_0128.jpg',
  source:       'dropbox-product',
  active: true, createdAt: Date,
}
```

`category` / `tags` 는 없다 — 컬렉션 자체가 '드롭박스 파일'이라 분류 칸이 필요 없다.

FTP 경로: `{FTP_REMOTE_DIR}/product/<슬러그>/<슬러그>_<4자리연번>.<확장자>` — **유지 확정**.

읽기는 `src/lib/queries.ts` 의 `getDropboxAssets()` / `getDropboxSummary()`,
쓰기는 `/api/dropbox` (GET 목록+집계 · PATCH 라벨확정 · DELETE 숨김).
**삭제는 없다** — 원본이 드롭박스에 그대로 있고 `sourcePath` 로 되돌아갈 수 있어서 숨김이면 충분하다.

## 7. 반드시 알아야 할 함정 6개

### ① cafe24 FTP 가 대량 업로드 중 연결을 끊는다
`ECONNRESET` 이 수백 장마다 터진다. 첫 실행은 이것 때문에 통째로 죽었다.
지금은 `withFtp()` 가 재연결 + 최대 4회 재시도로 감싸고 있고, **실제로 두 번 복구되는 것을 확인했다.**
업로드는 절대경로로 보낸다 — 재연결로 cwd 가 초기화돼도 엉뚱한 곳에 안 떨어지게.

### ② `lib/ftp.ts` 의 `sanitizeFilename` 은 한글 파일명을 파괴한다
`[^a-zA-Z0-9_-]` 를 전부 `_` 로 바꾸므로 한글명은 통째로 날아가 `upload_<타임스탬프>` 가 되고,
**같은 밀리초에 여러 장이면 덮어쓴다.** 그래서 이 스크립트는 파일명을 `<슬러그>_<연번>` 으로 직접 짓고
원본명은 `title` / `sourceName` 에 보존한다. 다른 대량 업로드를 만들 때도 같은 주의가 필요하다.

### ③ 드롭박스 파일이 전부 "온라인전용"이다
로컬 동기화 경로는 있지만 디스크 블록은 0이다(논리 8.7MB / 블록 0).
**읽는 순간 내려받아져 디스크를 차지한다.** 전량이면 약 48GB 인데 C: 여유는 51GB 밖에 없다.

회수 방법은 검증됐다 — **`attrib +U -P <경로> /S`** 로 온라인전용 복귀가 걸린다(속성에 `U` 확인).
스크립트의 `--unpin` 이 이걸 폴더 단위로 실행한다.
단 **되돌린 뒤 다시 읽으면 또 내려받아지므로, 업로드·검증이 끝난 폴더만 회수할 것.**

### ④ 샐리필은 제외 대상이다
4,947장으로 전체의 52% 인데 파일명 단서가 0이라 검증이 불가능하다. 사용자가 **"빼도 된다"** 고 명시했다.

---


### ⑤ cafe24 FTP 에 파일 크기 한계가 있다 — `552 Transfer aborted. File too large`
실측 2026-09-21: **16.6MB 는 통과, 53.5MB 는 거부.** 정확한 한계는 그 사이 어딘가다.
1차 배치 1,687장 중 18MB 초과는 14장(0.8%)뿐이라, 미리 다 줄이면 멀쩡한 사진까지 손해다.
→ `uploadOne()` 이 **원본으로 먼저 올리고 552 가 날 때만 줄여서 재시도**한다.
축소는 긴 변 4500px, 원본 포맷 유지(png 는 png 로 — 확장자와 내용이 어긋나면 안 된다).
줄인 건 `resized: true` / `originalBytes` / `uploadedBytes` 로 남는다. 1차 배치에서 7장이 이 경로를 탔다.
(어제 미디 265/270, 허기보 97/222 로 끝난 것도 일부는 이것 때문으로 보인다.)

### ⑥ sharp 가 큰 사진을 연속으로 줄이면 프로세스가 조용히 죽는다
**이것이 "exit 127 로 죽음" 의 정체다.** 실측 2026-09-21: 미디의 46~52MB 사진 3장을
연달아 축소하다 세 번째에서 종료 코드만 남기고 사라졌다(완료 줄도 안 찍힘).
원본이 12,769×9,579 = **122메가픽셀** 이라 raw 로 펴면 장당 약 367MB 인데,
libvips 가 기본적으로 디코딩 결과를 캐시에 쥐고 있어 연속 처리에서 쌓인다.

```js
sharp.cache(false);      // 한 장씩 처리하는 스크립트엔 캐시가 무의미하다
sharp.concurrency(1);    // 큰 이미지에서 스레드당 버퍼가 곱절로 든다
sharp(abs, { sequentialRead: true, limitInputPixels: 500_000_000, failOn: 'none' })
```
이 셋을 넣으니 같은 3장이 완주했다. **다른 대량 이미지 스크립트에도 같은 주의가 필요하다.**

## 8. 실행 방법

```bash
# 드라이런 — 계획만 출력 (1차 배치 전체)
node scripts/import-product-photos.mjs

# 한 폴더만 드라이런
node scripts/import-product-photos.mjs --folder=서포트

# 실제 업로드 + 등록
node scripts/import-product-photos.mjs --folder=서포트 --go

# 업로드 끝난 폴더 용량 회수(온라인전용 복귀)
node scripts/import-product-photos.mjs --folder=서포트 --go --unpin

# 등록된 URL 이 실제로 열리는지 검증
node scripts/check-product-photos.mjs          # 표본 4장
node scripts/check-product-photos.mjs --all    # 전량 (느림)
```

---

## 9. 남은 작업 (순서대로)

1. ~~3번 이슈를 사용자와 확정~~ **완료 2026-09-21** — 별도 컬렉션 + `/dropbox` 메뉴, FTP 경로 유지.
2. ~~이미 올라간 393장 마이그레이션~~ **완료** — `scripts/migrate-dropbox-assets.mjs --go`.
3. ~~1차 배치 나머지 1,294장 업로드~~ **완료 2026-09-21** — 1,687/1,687 · 실패 0.
   미디 270 · 허기보 222 · 더블 126 · 피라미드 115 · 팟 74 · 버블 31 · 라운저 166 · 서포트 683.
   용량 초과로 줄여 올린 것 7장. 검수 상태: 일치 406 · 단서없음 1,281 · 어긋남 0.
4. ~~전량 URL 검증~~ **완료** — 1,687장 전부 200, 실패 0.
5. ~~용량 회수~~ **완료** — 8개 폴더 전부 `--unpin` 실행.
   단 `attrib +U` 는 요청만 거는 것이고 실제 회수는 드롭박스가 백그라운드로 한다 — 바로 안 줄어든다.
6. **2차 배치** ← 여기부터. — 맥스 299 · 미니 125 · 드롭 715 · 도기보 754 · 모듀 63 · 메이트 46 · 오토만 24 · 프라임 26 · 신제품 290 · HDC영창 23. (약 2,365장)
7. **검수 대상 폴더는 따로** — 슬림 171 · 롤맥스 46 · 롤 닷 21 · 카터필러 63 · 인물 49. 폴더가 섞여 있으니 사람이 확인한 뒤 올릴 것.
8. ~~검수 UI~~ **완료** — `/dropbox` 화면이 그 일을 한다.
   폴더·검수상태(어긋남/단서없음/일치)·라벨유무로 거르고, 여러 장을 골라 한 번에 라벨을 찍는다.
   상단에 「제품 라벨 확정 N / M장」 진행률이 뜬다. 어긋남이 있는 폴더 칩에는 빨간 점이 붙는다.

## 10. 만들어진 화면 (2026-09-21)

- `src/app/dropbox/page.tsx` — 첫 200장만 서버에서, 나머지는 `/api/dropbox` 로 이어 받는다
- `src/components/DropboxManager.tsx` — 검수 도구. 타일 클릭 = 선택(확대는 ⤢ 버튼으로 새 탭)
- `src/app/api/dropbox/route.ts` — GET(목록+집계) · PATCH(라벨, 여러 장 동시) · DELETE(숨김)
- `src/lib/queries.ts` — `getDropboxAssets()` · `getDropboxSummary()` · `toDropboxAsset()`
- `src/components/Sidebar.tsx` — 자산 관리 > 레퍼런스 아래 「드롭박스」

확인한 것: 타입검사 통과, `/dropbox` 200, `/api/dropbox` 393장·집계 정상,
라벨 PATCH 한글 왕복 정상(쓰고 읽고 되돌림), 기존 `/references` 4,623장 정상.
